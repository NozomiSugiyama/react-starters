import Fab from "@material-ui/core/Fab";
import styled from "styled-components";

export default styled(Fab)`
    && {
        position: fixed;
        right: 0;
        bottom: 0;
        margin: 2rem;
    }
`;
